# Hello World Example

A simple repository containing a "Hello, World!" Python function.

## Usage

To run the hello world function directly:

```bash
python hello.py
```

To use the function in another Python file:

```python
from hello import hello_world
print(hello_world())  # Outputs: Hello, World!
```

for testing purposes...